import React from 'react';
import styles from './BuildControls.module.css';



const buildControls: React.FunctionComponent = (props) => {
    return (
        <div>

        </div>
    );
}

export default buildControls;